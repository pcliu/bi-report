# BI报告系统 - 用户使用文档

## 概述

BI报告系统提供网络流量的多维度分析，包括用户分析、应用分析、时间趋势等功能。

---

## 第一部分：安装部署（简版）

- 安装/运维细节参见 `INSTALL.md`，这里仅给最短路径：
```bash
python3 -m venv .venv && source .venv/bin/activate  # Windows 用 .venv\Scripts\activate
pip install uv && uv sync  # 或 pip install -r requirements.txt
streamlit run streamlit_dashboard.py
```
浏览器访问 `http://localhost:8501`，需要后台运行可参考 `INSTALL.md` 的 nohup 方案。

---

## 第二部分：数据文件管理

### 数据目录结构

所有数据文件存放在项目根目录下的 `data/` 文件夹：

```
bi-report/
├── data/                                              # 数据目录
│   ├── tbl_statistic_userapp_day_2025-12-08.csv      # 流量数据
│   ├── tbl_statistic_userapp_day_2025-12-10.csv      # 支持多个日期
│   ├── app_catagory_major.csv                        # 应用大类映射
│   └── app_catagory_minor.csv                        # 应用小类映射
├── bi_report.db                                       # SQLite数据库（自动生成）
└── streamlit_dashboard.py                            # 主程序
```

### 数据文件说明

| 文件 | 说明 | 格式 |
|------|------|------|
| `tbl_statistic_userapp_day*.csv` | 流量数据 | 无标题行，逗号分隔 |
| `app_catagory_major.csv` | 应用大类 | ID,名称 |
| `app_catagory_minor.csv` | 应用小类 | ID,名称 |

### 流量数据格式

```
用户账号,IP类型,应用大类ID,应用小类ID,上行流量,下行流量,总流量,时长,统计时间
example_user,0,4,1001,1024000,2048000,3072000,60000000,2025-12-08 00:00:00
```

- **IP类型**: 0=IPv4, 1=IPv6
- **流量单位**: 字节
- **时长单位**: 微秒

### 添加新数据

1. 将新的 CSV 文件复制到 `data/` 目录
2. 在系统侧边栏点击 **"🔄 从磁盘重新加载数据"**
3. 等待加载完成

### 更新数据

系统采用**全量替换**策略：
- 点击刷新按钮后，会清空现有数据
- 重新加载 `data/` 目录下的所有文件
- 确保 `data/` 目录包含完整数据

### 数据备份

```bash
# 备份数据目录
cp -r data/ data_backup_$(date +%Y%m%d)/

# 备份数据库
cp bi_report.db bi_report_backup_$(date +%Y%m%d).db
```

---

## 第三部分：系统使用

### 界面导航

启动后，系统会自动加载 `data/` 目录下的数据。

#### 侧边栏 - 控制面板

| 功能区 | 说明 |
|--------|------|
| **筛选条件** | 按用户、日期、IP类型、应用分类过滤数据 |
| **数据管理** | 显示数据状态，提供刷新按钮 |
| **报告导出** | 导出 PDF 完整报告 |

#### 主内容区 - 分析 Tab

| Tab | 功能 |
|-----|------|
| **🌊 流量分析** | 上下行流量分布、TOP用户、流量时长 |
| **👥 用户分析** | 用户活跃度、流量消耗分布、高上传用户 |
| **📱 应用分析** | 应用大类/小类流量排行 |
| **⏰ 时间分析** | 流量时间趋势，支持多维度分组 |

### 筛选功能

| 筛选项 | 说明 |
|--------|------|
| 用户筛选 | 下拉选择特定用户账号 |
| 日期范围 | 选择开始和结束日期 |
| IP 类型 | 全部 / IPv4 / IPv6 |
| 应用大类 | 按应用分类过滤 |

### 分析功能

#### 流量分析
- 上行vs下行饼图
- IPv4 vs IPv6 分布
- 流量时长分布
- TOP用户排行

#### 用户分析
- 用户活跃度分布
- 流量消耗分布
- 高活跃上传用户识别
- 上传比例分析

#### 应用分析
- 应用大类流量排行
- 应用小类流量排行
- 各应用用户数统计

#### 时间分析
- 支持按总体/IP类型/应用/用户分组
- 支持会话数/流量/时长等指标

### PDF 报告导出

点击 **"📄 导出完整PDF报告"** 生成包含所有图表的专业报告。

---

## 第四部分：常见问题

### 启动报错 "ModuleNotFoundError"
**解决**: 确保已激活虚拟环境
```bash
source .venv/bin/activate  # Linux
.venv\Scripts\activate      # Windows
```

### 页面显示为空
**解决**: 检查 `data/` 目录是否有 CSV 文件，或点击刷新按钮

### PDF 导出失败
**解决**: 安装 kaleido
```bash
pip install kaleido
```

### 端口被占用
**解决**: 使用其他端口
```bash
streamlit run streamlit_dashboard.py --server.port 8502
```

### 图表交互技巧
- **悬停**: 显示详细数据
- **点击图例**: 隐藏/显示系列
- **双击图例**: 仅显示该系列
- **拖拽**: 框选放大

---

## 第五部分：联系与支持

如有问题，请联系系统管理员或查阅项目 README 文档。
